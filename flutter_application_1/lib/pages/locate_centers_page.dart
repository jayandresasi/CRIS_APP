import 'package:flutter/material.dart';

import 'abtc_map_screen.dart';

/// Backwards-compatible entry point used by the dashboard and drawer.
class LocateCentersPage extends StatelessWidget {
  const LocateCentersPage({super.key});

  @override
  Widget build(BuildContext context) => const ABTCMapScreen();
}
